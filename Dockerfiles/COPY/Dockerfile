FROM cmd:v1
COPY index.html /usr/share/nginx/html/
EXPOSE 80

## Why This Works Perfectly

cmd:v1 already has:
✅ almalinux:9 as base
✅ nginx installed
✅ CMD to start nginx

We just ADD our own webpage using COPY!